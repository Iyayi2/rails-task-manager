<h3 class="details my-2"><%= @task.details %></h3>
<div class="show_page my-2">
  <div class="btn btn-secondary"><%= link_to 'Edit task', tasks_edit_path(@task.id) %></div>

  <div><%= link_to "Delete",
    task_path(@task.id),
    data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
  %></div>
</div>
